# Production Environment

# Real production builds should call your backend API, not the static demo mock.
VITE_USE_MOCK=false
VITE_DEMO_MODE=false

# Replace this with your production backend URL before deploying a real project.
VITE_API_BASE_URL=https://api.example.com
